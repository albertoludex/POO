package examenes;

public interface ImprimirI {
	/**
	 * Método que imprime un objeto con formato. (Versión Simle).
	 * @return String cadena que se impirmirá.
	 */
	 String print_simple();
	 
	 
	 /**
	  * Método que imprime un objeto con formato. (Versión Completa).
	  * @return String cadena que se imprimirá;
	  */
	 String print_comp();

}
